# 视频自动化工作台 · 团队每日操作指南

---

## 🖥️ 你的操作（主机/服务器，唯一一台要开服务的电脑）

### 每天开工，只需 3 步：

**第 1 步**：双击桌面的 `启动工作台.bat`

> 弹出一个黑窗口，看到 `Uvicorn running on http://0.0.0.0:8000` 就说明好了。
> 这个窗口**不要关**，可以最小化到任务栏。

**第 2 步**：确认你的 IP 地址

黑窗口启动时会打印类似：
```
  LAN:
    http://192.168.1.6:8000
```
把 `http://192.168.1.6:8000` 发给同事（如果 IP 没变，每天一样）。

**第 3 步**：打开浏览器验证

你自己访问 `http://localhost:8000`，确认页面能正常打开。

> ⚠️ 下班时关掉黑窗口即停止服务。同事就无法访问了，所以如果你先走，提前跟同事打招呼。

### 每天收工：

关闭 `启动工作台.bat` 的黑窗口即可。

---

## 👥 同事操作（台式机，打开浏览器就行）

### 每天开工，2 步：

**第 1 步**：确认在你的电脑上看到了类似这个地址：
```
http://192.168.1.6:8000
```
（每天 IP 可能一样也可能变，以你当天发的为准）

**第 2 步**：浏览器打开这个地址，开始干活。

> 没有任何安装、没有黑窗口、不用装任何东西。就是一个网页。

---

## 🔁 如果某天同事打不开了

**第 1 步**（你）：在黑窗口里看打印的 LAN IP 有没有变化，把新的发给同事。

**第 2 步**（你）：如果 IP 没变但还是打不开，关掉黑窗口 → 重新双击 `启动工作台.bat`。

**第 3 步**：还是不行？用你的手机开热点方案：
- 打开 Windows **设置** → **网络和 Internet** → **移动热点** → 打开开关
- 同事连上你的热点 WiFi
- 访问 `http://192.168.137.1:8000`

---

## 📋 一句话版

| 角色 | 做什么 |
|------|--------|
| **你** | 双击 `启动工作台.bat`，把 IP 发给同事，黑窗口别关 |
| **同事** | 浏览器打开你给的地址 |
| **收工** | 你关黑窗口 |
