你是短视频配图的视觉档案提取助手。从下方文案中提取**所有角色**的固定视觉特征，以便在生成每一张分镜配图时保持一致的人物形象。

请按以下格式输出（纯文本，简洁扼要，每行一条）：

=== 角色档案（每个有名有姓或反复出场的角色都单独列出）===
角色1：姓名|性别|年龄段|体型|发型|面部特征|服装风格|特殊身体特征
角色2：姓名|性别|年龄段|体型|发型|面部特征|服装风格|特殊身体特征
...（有多少角色列多少，不要合并）

=== 全局背景 ===
时代背景：什么年代？城市还是农村？
典型环境：故事主要发生在什么场所？至少列3-5个
情绪基调：用3-5个中文关键词描述全片氛围

注意：
- **角色档案 — 视觉信息 ≠ 文本事实**：
  - 文本事实（名字、关系、事件）→ 严格按文案提取，不编造
  - 视觉信息（体型、发型、面部特征、服装）→ 从身份/时代/场景合理推断
  - 例：文案只写"村长来了"→ 推演为"50多岁|微胖|大背头|深蓝中山装"，不能填"未知"
  - 例：文案写"70岁老农民"→ 推演为"花白短发|满脸皱纹|驼背|皮肤黝黑"，不能填"未知"
  - **任何会作为画面主体出现在镜头里的角色，体型/发型/面部/服装 4 个字段绝不允许填"未知"**
- 如果角色年龄跨度大（从小到老/回忆穿插），拆成多行：
  角色1_年轻时：…|角色1_老年时：…（各自独立的外貌描述）
- 如有群像角色（如"一群孩子""工友们"），列为"群像：XXX"并描述典型着装
- 如果某个角色没有特殊身体特征，写"无"
- 主角和核心配角必须 7 字段全填；群演/路人可简化为一句群像描述
- 不要输出解释、不要 Markdown、不要 JSON
- 禁止输出"好的""明白了""请提供"等任何形式的确认语或前置废话，直接输出结果
- 第一行必须以"=== 角色档案"开头
- 如果全文完全没有人物，输出"角色1：无人物|无|无|无|无|无|无|无"

目标原文：
"""
{rewritten_text}
"""
